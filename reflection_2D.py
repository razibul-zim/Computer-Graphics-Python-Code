def reflect_over_x_axis(x, y):
    return x, -y

# Example:
point = (3, 4)
reflected = reflect_over_x_axis(point[0], point[1])
print(f"Original point: {point}")
print(f"Reflected over x-axis: {reflected}")
